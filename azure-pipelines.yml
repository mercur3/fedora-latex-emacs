
###############################################################################
# WARNING                                                                     #
# This file is autogenerated. Do NOT edit this file. Run =./build.py= instead #
###############################################################################

trigger:
  - master
pr:
  autoCancel: true
  branches:
    include:
      - master
pool:
  vmImage: ubuntu-latest
jobs:
  - job:
    displayName: Building on Fedora 38
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: |
          echo "Building the images"
          printf "Using %d threads\n" $(nproc)
          cd build
          echo "------------------------------------------------------\n"
          docker build -f Dockerfile.38-base   -t mercur3/fedora-latex-emacs:38-base   .
          docker build -f Dockerfile.38-medium -t mercur3/fedora-latex-emacs:38-medium .
          docker build -f Dockerfile.38-full   -t mercur3/fedora-latex-emacs:38-full   .
          echo "------------------------------------------------------\n"
        displayName: docker build -f Dockerfile -t mercur3/__tag_name__ .

      - script: |
          echo "------------------------------------------------------\n"
          echo "$DOCKER_PASSWORD" | docker login -u mercur3 --password-stdin
          docker push mercur3/fedora-latex-emacs:38-medium
          docker push mercur3/fedora-latex-emacs:38-full
        displayName: docker push mercur3/__tag_name__
        condition: not(eq(variables['Build.Reason'], 'PullRequest'))
        env:
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)


  - job:
    displayName: Building on Fedora 39
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: |
          echo "Building the images"
          printf "Using %d threads\n" $(nproc)
          cd build
          echo "------------------------------------------------------\n"
          docker build -f Dockerfile.39-base   -t mercur3/fedora-latex-emacs:39-base   .
          docker build -f Dockerfile.39-medium -t mercur3/fedora-latex-emacs:39-medium .
          docker build -f Dockerfile.39-full   -t mercur3/fedora-latex-emacs:39-full   .
          echo "------------------------------------------------------\n"
        displayName: docker build -f Dockerfile -t mercur3/__tag_name__ .

      - script: |
          echo "------------------------------------------------------\n"
          echo "$DOCKER_PASSWORD" | docker login -u mercur3 --password-stdin
          docker push mercur3/fedora-latex-emacs:39-medium
          docker push mercur3/fedora-latex-emacs:39-full
        displayName: docker push mercur3/__tag_name__
        condition: not(eq(variables['Build.Reason'], 'PullRequest'))
        env:
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)


  - job:
    displayName: Building on Fedora 40
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: |
          echo "Building the images"
          printf "Using %d threads\n" $(nproc)
          cd build
          echo "------------------------------------------------------\n"
          docker build -f Dockerfile.40-base   -t mercur3/fedora-latex-emacs:40-base   .
          docker build -f Dockerfile.40-medium -t mercur3/fedora-latex-emacs:40-medium .
          docker build -f Dockerfile.40-full   -t mercur3/fedora-latex-emacs:40-full   .
          echo "------------------------------------------------------\n"
        displayName: docker build -f Dockerfile -t mercur3/__tag_name__ .

      - script: |
          echo "------------------------------------------------------\n"
          echo "$DOCKER_PASSWORD" | docker login -u mercur3 --password-stdin
          docker push mercur3/fedora-latex-emacs:40-medium
          docker push mercur3/fedora-latex-emacs:40-full
        displayName: docker push mercur3/__tag_name__
        condition: not(eq(variables['Build.Reason'], 'PullRequest'))
        env:
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)


